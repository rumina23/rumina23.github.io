# Selection is use to control the flow of the program



score = 26

userNum = 25 # int(input("Enter user num: "))

if score > userNum: #check the condition that score is greater than 25

    # do something/ do tho / execute this line of code

    print(f"Good score {score}")

    if score < userNum:
    print(f"Good score {score}")
    print(2 * 5)


    if score <= userNum:
    print(f"Good score {score}")