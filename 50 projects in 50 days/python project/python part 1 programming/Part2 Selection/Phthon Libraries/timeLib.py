import time # import the time library/class/module

name = "Jane"
print(name)

#invoke/call the time function and pass in seconds as argument
time.sleep(5)
print(f"This will be printer after 5 seconds: {name}")